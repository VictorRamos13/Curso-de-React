function printElementsForEach(arr) {
    let output = [];
    arr.forEach(element => {
        output.push(element);
    });
    return output; // Returning array of printed elements for testing purposes
}